// Operador Condicional Ternário
let bread = true
let cheese = false
const theGoodBreakFast = bread || cheese ? "So good" : "Not so good"
console.log(theGoodBreakFast)

let age = 18
const verificationAge = age >= 18 ? "Maior de idade" : "Menor de idade"
console.log(verificationAge)