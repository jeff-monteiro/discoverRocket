## Scope var
É uma variável de escopo global que pode ser acessada de qualquer parte do programa.

## Scope let and const
São variáveis que só podem ser acessadas a partir de seu escopo local.

*** Example ***
Escopo é representado por {};

{
  let nome = "Jefferson"
  console.log(nome)
}